
  # Student Trading Dashboard

  This is a code bundle for Student Trading Dashboard. The original project is available at https://www.figma.com/design/yorUtMHNrkudoLIrscMx2f/Student-Trading-Dashboard.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  